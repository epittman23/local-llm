# Rich table/markdown rendering for the llama-console CLI helpers
# (scripts/llama_console.py: lllm-profiles, lllm-check, lllm-vram). Degrades
# to plain stdlib output when absent, so the venv is a convenience, not a
# hard dependency. Textual was here for the Textual dashboard (llama-ui); it
# was retired for the web dashboard (llama-web) on 2026-09-06 and dropped
# from here in the same change.
#
# requirements-extra.txt is gone: it held numpy/pandas/pyyaml/scipy/
# matplotlib/fastapi/uvicorn for llama-web/llama-test/llama-compare/
# llama-report/llama-tune, all retired when that suite moved into the Open
# WebUI fork's own backend -- see docs/CLAUDE.md's decisions log. Those
# dependencies now live in open-web-ui/openwebui/backend/requirements.txt
# instead.
rich>=13.7
