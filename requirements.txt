# Rich table/markdown rendering for the llama-* CLI helpers
# (scripts/llama_console.py, used by llama_test.py, llama_compare.py,
# llama_tune.py and llama_web.py). Every entry point degrades to plain stdlib
# output when this is absent, so the venv is a convenience, not a hard
# dependency. Textual was here for the Textual dashboard (llama-ui); it was
# retired for the web dashboard (llama-web, requirements-extra.txt) on
# 2026-09-06 and dropped from here in the same change.
rich>=13.7

# DS-1000 grading only. Its problems are real StackOverflow pandas/numpy tasks
# and its code_context harness imports these directly; without them those items
# are recorded as skipped rather than failed. HumanEval and MBPP need nothing
# beyond the standard library.
numpy>=1.26
pandas>=2.1

# DS-1000 items occasionally round-trip through YAML. One module, and it is the
# difference between an item being gradeable here and being dropped.
pyyaml>=6.0
